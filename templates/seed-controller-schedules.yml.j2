---
# Example schedules configuration — rename to .yml to activate.
# This file belongs in base/schedules/ and applies to ALL environments.
# For environment-specific overrides, place a .yml file in <env>/schedules/.
# Full field reference: {{ scm_base_url }}/{{ platform_scm_org }}/{{ engine_repo | default('aap-casc-engine', true) }}{{ '/-/blob/' if (scm_provider | default('github')) == 'gitlab' else '/blob/' }}main/docs/RESOURCE_CATALOG.md#controller_schedules
controller_schedules:
  - name: sch-{{ tenant_id }}-example
    description: "Example schedule — replace with your schedule"
    unified_job_template: jt-{{ tenant_id }}-example
    organization: {{ _effective_aap_organization | to_json }}
    rrule: "DTSTART;TZID=UTC:20260101T060000 RRULE:FREQ=WEEKLY;BYDAY=MO"
    enabled: false
