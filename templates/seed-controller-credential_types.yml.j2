---
controller_credential_types:
  - name: "Example Custom Credential Type"
    description: "Example credential type — replace with your type"
    kind: cloud
    inputs:
      fields:
        - id: api_key
          type: string
          label: API Key
          secret: true
    injectors:
      extra_vars:
        my_api_key: "{% raw %}{{ api_key }}{% endraw %}"
