---
collections:
  - name: infra.aap_configuration
    version: ">=2.9.0"

  # Transitive dependencies (pulled automatically by infra.aap_configuration):
  #   ansible.platform, ansible.controller, ansible.hub, ansible.eda
  # These are Red Hat Certified collections bundled with AAP execution
  # environments. Do NOT list them explicitly — their version constraints
  # are managed by infra.aap_configuration's own metadata.
